---
name: landing-generation
description: "Step 6 + 7a–c of the landing pipeline — build the landing: pick a landing pattern, auto-select the stack, generate code with the 7 critical rules (H1 on the primary barrier, no base64, WebP, anti-AI-slop, forms via Apps Script, footer year, flexible structure), legal pages by region, then SEO/GEO + schema.org, mobile (375/768/1024, CWV), PageSpeed fixes. Use when the user says 'собери лендинг', 'сгенерируй код лендинга', 'проверь лендинг по чек-листу', 'SEO для лендинга', 'PageSpeed'."
---

1. Прочитай `06-landing-generation-and-polish.md` и `LANDING-RULES.md` в этой папке, выполни шаг по ним.
2. Перед выдачей кода прогони pre-flight чек-лист (`LANDING-RULES.md`, раздел 9) и `PITFALLS.md`.
3. Результат — `landing.html` (или scaffold стека), legal-страницы, `images/`, `seo-patches.md`, `mobile-patches.md`, `pagespeed-analysis.md` в папку проекта, отметка в `STATUS.md`.
4. Деплой и формы — в исходном репозитории `../DEPLOY.md`, после установки — `DEPLOY.md` рядом с этим файлом.
